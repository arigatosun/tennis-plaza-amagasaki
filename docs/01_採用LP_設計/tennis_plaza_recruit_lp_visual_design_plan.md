# テニスプラザ尼崎 採用LP ビジュアル / UIUX設計書

作成目的：実装前に、採用LPの第一印象・モーション・コンポーネント方針を固める。既存の `tennis_plaza_recruit_lp_implementation_plan.md` のコピー/構成を活かしつつ、react-bits を利用して「採用ブランドとして印象に残る」「テニス業界らしくプレイフル」「MTGデモで一目で強い」LPに引き上げる。

参照：

- React Bits GitHub: https://github.com/DavidHDev/react-bits
- React Bits Docs: https://reactbits.dev/
- React Bits component index: https://reactbits.dev/get-started/index

---

## 1. デザインコンセプト

### 1-1. コア表現

```text
Rally the Future.
テニス経験が、スクールを動かす力に変わっていく。
```

日本語の主コピーは既存計画書のまま以下を採用する。

```text
テニスを教えるだけではなく、
スクールを育てる仕事。
```

ビジュアル面では「静的な求人ページ」ではなく、以下の体験を目指す。

- コート上のラリーのように、視線がテンポよく次の情報へ流れる
- テニスボール、コートライン、ラケットのスイング、ステップワークを抽象的なモーションに変換する
- 採用ページとしての信頼感は保ちつつ、若い求職者が「ちょっと格好いい」と感じる演出を入れる
- AIロープレや育成構想は未来感を出すが、テック企業っぽくなりすぎない

### 1-2. 目指す印象

```text
プレイフル / 速い / 現場感 / 成長感 / 少し未来的 / でも誠実
```

避ける印象：

- 普通の求人票
- 体育会系すぎる熱血
- 美容・SaaS・AI企業のような汎用的なグラデーションLP
- AI生成素材っぽすぎる広告感
- モーションが多すぎて本文が読みにくいページ

---

## 2. React Bits 採用方針

React Bits は、公式README上で「110+の text animations / UI elements / backgrounds」「minimal dependencies」「customizable」「copy-paste ready」と説明されている。今回のLPではライブラリ全体を入れるのではなく、必要なコンポーネントだけをコピー/CLI導入して使う。

### 2-1. 採用するReact Bitsカテゴリ

| カテゴリ | 用途 | 採用理由 |
|---|---|---|
| Text Animations | Hero、見出し、強調コピー | 第一印象を一気に上げる。コピーが強いLPなので効果が大きい |
| Backgrounds | Hero、AIロープレ、セクション間 | テニスのスピード感や未来感を抽象表現できる |
| Components | 価値カード、仕事内容、キャリアパス | 情報量の多いLPを「動きのある見せ場」に変える |
| Animations | CTA、カーソル、画像、カード | プレイフルさを足し、クリックしたくなる導線を作る |

### 2-2. 候補コンポーネント

実装時に優先して検討するReact Bitsコンポーネント：

| React Bits | 使う場所 | 使い方 |
|---|---|---|
| `SplitText` | Hero H1 | 1行ずつ、または文字単位で軽く立ち上げる |
| `BlurText` | Heroサブコピー / 共感ブロック | ぼかし解除で読み始めの引きを作る |
| `ScrollReveal` | 各セクション本文 | 長文をスクロールに合わせて自然に出す |
| `ScrollFloat` | セクション見出し | 見出しに軽い浮遊感。多用しすぎない |
| `ScrollVelocity` | Hero下のキーワード帯 | 「未経験OK / 研修あり / 運営まで広がる」などを流す |
| `ShinyText` | CTA内の短いアクセント | CTAを少し特別に見せる |
| `StarBorder` | Primary CTA | テニスボール色の境界アニメーションに調整 |
| `ClickSpark` | Desktopのみ | クリック時にテニスボールの弾けるような反応 |
| `Magnet` | CTA / 重要カード | ホバー時の吸着感で気持ちよさを出す |
| `SpotlightCard` | 3つの価値 / 仕事内容 | カードの読みやすさとインタラクションを両立 |
| `TiltedCard` | 写真カード | コート写真に軽い奥行きを出す |
| `CardSwap` | Hero右側 / メッセージ | コーチ、子ども、保護者、スタッフの場面を切り替える |
| `ScrollStack` | キャリアパス | キャリアが積み上がっていく体験に合う |
| `Threads` | Hero背景の薄い線 | ラリー軌道やコート空気感として使う |
| `GridMotion` | 仕事内容 / キャリア背景 | コートラインの抽象表現 |
| `LightRays` | Hero / Final CTA | 屋外コートの自然光を演出 |
| `GridScan` | AIロープレ | 未来構想だけ少しデジタル感を加える |

使わない、または慎重に扱うもの：

- `Orb` 系：装飾の丸い発光体に寄ると汎用AI/SaaS感が出るため不採用
- `Hyperspeed` / `Lightning`：スポーツ感はあるが、採用LPとしては激しすぎる
- `LetterGlitch`：AIロープレに使えるが、誠実さを損なう可能性があるため控えめ
- `FaultyTerminal`：テニススクール採用の文脈から外れる

---

## 3. ビジュアルシステム

### 3-1. カラーパレット

既存計画書の緑と黄色を軸にしつつ、単調な緑LPにならないようアクセントを増やす。

| 用途 | 色 | 使い方 |
|---|---|---|
| Deep Court | `#0B3B2E` | ヘッダー、濃色セクション、本文の強調 |
| Rally Green | `#18A36B` | 見出しアクセント、リンク、ライン |
| Tennis Yellow | `#D9FF43` | CTA、重要な点、モーション粒子 |
| Clay Coral | `#FF7A59` | 小さな差し色。スポーツ感と温度感 |
| Sky Blue | `#7DD3FC` | AIロープレや未来構想の補助色 |
| Warm White | `#F8F6EF` | 基本背景 |
| Ink | `#17212B` | 本文 |

注意：

- メイン背景を緑一色にしない
- 黄色はCTAと粒子に限定し、面積を増やしすぎない
- 紫/青系グラデーションLPには寄せない

### 3-2. タイポグラフィ

| 用途 | 方針 |
|---|---|
| 日本語本文 | Noto Sans JP。読みやすさ優先 |
| 英字アクセント | Inter / Manrope。スポーツブランド感を出す |
| Hero H1 | 太め、行間狭め。ただし文字詰めは通常 |
| セクション見出し | Heroより小さく、情報UIとして読みやすく |
| CTA | 14-16px、太字、アイコン併用 |

Heroの英字アクセント例：

```text
RECRUIT / COACH CAREER
RALLY THE FUTURE
SCHOOL GROWTH CREW
```

### 3-3. 形状

- カード角丸は基本 `8px`
- CTAは丸ボタンでよいが、本文カードまで丸くしすぎない
- コートライン由来の直線・角・グリッドを多用する
- 写真や動画は大きく、カードの中に閉じ込めすぎない

---

## 4. モーション設計

### 4-1. モーションの基本ルール

採用LPなので「激しい」だけではなく、以下のリズムで制御する。

```text
Hero：強い。最初の3秒で印象を作る
Middle：読みやすい。カードや見出しだけ動く
Career / AI：体験的。スクロール連動で少し派手
Final CTA：再び強い。応募導線に熱量を戻す
```

### 4-2. モーションメタファー

| テニスの動き | UI表現 |
|---|---|
| サーブ | Hero H1 のSplitText立ち上がり |
| ラリー | ScrollVelocityのキーワード流れ |
| ボールの軌道 | Threads / LightRays の細い線 |
| ステップワーク | カードの stagger reveal |
| スピン | CTA hover / ClickSpark |
| コートライン | GridMotion / section divider |

### 4-3. アクセシビリティ

- `prefers-reduced-motion` ではアニメーションを停止またはフェード程度に落とす
- Hero背景動画/Canvasは停止しても情報が伝わる静止画を必ず置く
- CTAやFAQなど操作UIはモーションなしでも状態が分かる
- テキストはアニメーション完了後に通常DOMテキストとして読める状態にする

### 4-4. パフォーマンス

- Canvas背景はHeroとAIセクションに限定
- モバイルでは背景Canvasを軽量版または静止画に切り替える
- `ScrollReveal` 系はIntersection Observerで発火
- 複数の重い背景を同時に表示しない
- 画像はWebP/AVIF、HeroはLCP対象として優先読み込み

---

## 5. セクション別 UIUX設計

### Section 0: Sticky Header

目的：スクロール中も見学導線を失わせない。

デザイン：

- 背景は白半透明 + blur
- 左にロゴ、中央にアンカー、右にCTA
- スクロール後は高さを少し圧縮
- CTAに `StarBorder` か軽い `ShinyText`

モーション：

- 初期表示は上から短く slide-down
- CTA hover は `Magnet`
- Mobileは下部Sticky CTAを主役にする

### Section 1: Hero / First View

目的：一瞬で「普通のテニスコーチ求人ではない」と伝える。

現行計画の2カラムではなく、第一印象を強めるため以下に変更する。

```text
Full-bleed hero image/video
  + dark green readable overlay
  + animated court lines / rally trails
  + large H1
  + two CTA
  + moving keyword ticker
```

構成：

- 背景：実写風のテニススクール写真/短尺ループ動画
- 上層：`Threads` または `LightRays` を薄く重ねる
- H1：`SplitText`
- サブコピー：`BlurText`
- バッジ：未経験OK / 研修あり / 社員月給21.2万円〜
- Hero下部：`ScrollVelocity` で「教育 / 接客 / 入会案内 / 運営 / 育成 / 成長」を流す

見た目：

- Heroは画面高 `min-height: 86svh`
- 次セクションの一部が見えるよう、下に共感ブロックの先頭を少し覗かせる
- 文字はカードに入れず、背景上に直接配置

### Section 2: 共感ブロック

目的：求職者の不安を先に言い当てる。

デザイン：

- Warm White背景
- 4つの不安カードを `SpotlightCard`
- それぞれに小さなアイコン
- カード角丸は8px

モーション：

- 見出しは `BlurText`
- カードはstaggerで下から出る
- hoverでライトが追従。ただし本文は揺らさない

### Section 3: 採用ブランドコンセプト

目的：LPの中心メッセージを強く記憶させる。

デザイン：

- 白背景に巨大な薄いコートライン
- 中央にコピー
- 強調コピー「テニス経験を、人を動かす力に変える。」は `ScrollFloat`

モーション：

- スクロールでコートラインが少しだけ伸びる
- 文字は遅延をつけて表示

### Section 4: 3つの価値

目的：「ここで働く理由」を短く整理する。

デザイン：

- `MagicBento` または `SpotlightCard`
- 3カードを横並び
- 番号はテニススコア風に大きく表示
- Clay Coralを小さく差す

モーション：

- hover時にカード境界が黄色く走る
- アイコンは軽く回転/跳ねる

### Section 5: 山下コーチの教育哲学

目的：山下社長に「分かっている」と感じてもらう。

デザイン：

- 左右分割ではなく、写真を大きく敷いた editorial セクション
- 文章は右寄せの読みやすいカラム
- Quoteは白背景の小さな引用パネル

モーション：

- 写真は `TiltedCard` で軽く奥行き
- Quoteだけ `ScrollReveal`

注意：

- 実人物に似せたAI画像は使わない
- MTGでは「本人写真・本人の言葉に差し替える」前提を明記

### Section 6: 近江まやの 現場メッセージ

目的：現場の安心感を出す。

デザイン：

- Staff photo / courtside scene を大きく
- メッセージはチャット/メモ風ではなく、現場責任者の言葉として端正に
- `CardSwap` で「レッスン外の価値」を3場面に切り替える案もあり

モーション：

- 写真カードに軽い hover tilt
- 本文は読みやすさ優先で控えめ

### Section 7: 仕事内容

目的：「営業できるコーチ」を嫌がられない表現に翻訳する。

デザイン：

- 6カード構成
- `SpotlightCard`
- アイコンは lucide-react で統一
- 「営業」ではなく「価値を伝える入会案内」と表示

モーション：

- カードhoverでスポットライト
- click/hoverで小さな `ClickSpark`

### Section 8: 1日の流れ

目的：仕事のリアルを見せる。

デザイン：

- コートライン風タイムライン
- 時刻はテニススコアボード風
- 背景に薄い `GridMotion`

モーション：

- スクロールでラインが伸びる
- 各時間が順に点灯

### Section 9: キャリアパス

目的：将来性を強く見せる。

デザイン：

- `ScrollStack` でキャリアカードが積み上がる
- Step 3「体験・入会フォロー担当」を必ず強調
- 最終カードはSchool Growthとして少し広く見せる

モーション：

- スクロール連動でカードが重なり、役割が増える感覚を作る
- 最終コピーは `ScrollFloat`

### Section 10: 育成体制

目的：未経験OKを信頼できる形で示す。

デザイン：

- 研修項目はチェックリストではなく「Training Modules」として見せる
- `AnimatedList` またはstagger reveal
- 横にコーチング写真

モーション：

- モジュールが順に表示
- 完了チェックがテニスイエローで点灯

### Section 11: AIロープレ構想

目的：アリガトサンの先の提案力を見せる。

デザイン：

- Deep Court背景
- `GridScan` を薄く敷く
- ロープレシーンを会話カードとして並べる
- Sky Blueはここだけ補助的に使う

モーション：

- 会話カードが順に入力されるように出る
- `TextType` を一部に使ってもよい

注意：

- AIを主役にしすぎない
- 「採用後の育成を支える構想」として控えめに置く

### Section 12: 求める人物像

目的：セルフスクリーニング。

デザイン：

- 二分割：「向いている人」「少し合わないかもしれない人」
- 否定表現は柔らかく
- チェックリストは読みやすく

モーション：

- 控えめな `ScrollReveal`
- 激しい演出は不要

### Section 13: 募集要項

目的：条件を分かりやすく表示する。

デザイン：

- テーブルはシンプルに
- 条件確認が必要な箇所は脚注で明示
- ここではモーションを抑えて信頼感を優先

モーション：

- 行が軽くfade-in
- hover装飾は不要

### Section 14: 選考フロー

目的：応募ハードルを下げる。

デザイン：

- 5ステップを横/縦レスポンシブ
- 最初を「見学・カジュアル面談」にする
- CTAを再配置

モーション：

- スクロールでステップが1つずつアクティブ化

### Section 15: FAQ

目的：不安を減らす。

デザイン：

- Accordion
- 開閉時に高さアニメーション
- FAQ openイベントを取れるようにする

モーション：

- アイコン回転程度
- 読む場所なので演出は控えめ

### Section 16: Final CTA

目的：見学・面談へ背中を押す。

デザイン：

- Heroと呼応する濃色背景
- `LightRays` を薄く重ねる
- 仮フォーム領域を設置
- CTAは `StarBorder` + `Magnet`

モーション：

- 見出しは `BlurText`
- CTA周辺にテニスイエローの小さなラインアニメーション

---

## 6. 画像・動画方針

### 6-1. Hero

最も重要。静止画でも動画でも、以下を満たす。

- 日本のテニススクールらしい現場感
- コーチが子どもに目線を合わせている
- 背景にレッスンの活気
- 保護者が安心して見守る雰囲気
- 過度な広告写真感を避ける

### 6-2. Section写真

- 山下コーチ哲学：経験豊かなコーチが一人に向き合う場面
- 近江さんメッセージ：現場スタッフ/コーチが生徒やスタッフと話す場面
- 育成体制：先輩が若手にフィードバックする場面
- AIロープレ：コート横でタブレットを見ながら練習する場面

### 6-3. 実人物素材の注意

- 山下社長・近江さん本人写真は許諾後に差し替える
- AI素材は本人に似せない
- デモ時は「仮素材」と分かる管理にする

---

## 7. 実装方針

### 7-1. ファイル構成

```text
/src/app/recruit/page.tsx
/src/data/recruit.ts
/src/components/recruit/*
/src/components/react-bits/*
/public/images/recruit/*
/public/videos/recruit/*
```

React Bits由来のコンポーネントは `src/components/react-bits/` に配置し、プロジェクト側のセクションコンポーネントから呼び出す。

### 7-2. 導入順

1. デザイン設計に沿って既存コピー/構成を再整理
2. HeroをFull-bleed構成に変更
3. `SplitText`, `BlurText`, `ScrollReveal` を先に導入
4. `SpotlightCard`, `Magnet`, `ClickSpark` をCTA/カードに導入
5. `Threads` / `LightRays` / `GridScan` など背景系を必要セクションだけに追加
6. BrowserでPC/SPを確認し、文字被り・重さ・読みづらさを調整

### 7-3. イベント設計

既存計画書のイベント名を維持する。

```text
recruit_cta_click
recruit_scroll_depth
recruit_faq_open
recruit_video_play
```

追加検討：

```text
recruit_motion_loaded
recruit_hero_view
```

---

## 8. Motion Budget

| 場所 | 強さ | 理由 |
|---|---:|---|
| Hero | 5/5 | 第一印象を決める |
| 共感ブロック | 3/5 | 読みやすさを保ちながら動かす |
| 価値カード | 4/5 | 働く理由を印象づける |
| 山下/近江メッセージ | 2/5 | 信頼感が主役 |
| 仕事内容 | 3/5 | 情報量を軽く見せる |
| キャリアパス | 5/5 | 将来性を体験として見せる |
| 育成体制 | 3/5 | 安心感を出す |
| AIロープレ | 4/5 | 未来構想の見せ場 |
| 募集要項 | 1/5 | 正確さと信頼感優先 |
| FAQ | 1/5 | 読む場所 |
| Final CTA | 5/5 | 最後に熱量を戻す |

---

## 9. 完成イメージ

このLPの見た目は、一般的な採用ページではなく「地域密着テニススクールの採用ブランドサイト」にする。

求職者の第一印象：

```text
テニスコーチの求人なのに、ちゃんと今っぽい。
教えるだけじゃなく、スクールを動かす仕事なんだ。
ここなら自分のテニス経験をキャリアにできそう。
```

山下社長・近江さんの第一印象：

```text
ただ求人ページを作るのではなく、
採用したい人にどう見せるかまで設計されている。
媒体に出す前に、この採用ブランドを整える意味がありそう。
```

アリガトサンとしての見え方：

```text
採用ブランド設計、LP、求人票、スカウト文、育成ロープレまで一気通貫で提案できる。
```

---

## 10. 実装前の決定事項

次の実装では以下を採用する。

1. Heroは2カラムではなく、Full-bleedビジュアル + テキストオーバーレイに変更する
2. React Bitsは必要コンポーネントだけを `src/components/react-bits/` に導入する
3. Motionは強めにするが、募集要項・FAQ・長文本文は読みやすさを優先する
4. Desktopはかなり動かし、Mobileは軽量化する
5. `prefers-reduced-motion` 対応を必ず入れる
6. カード角丸は基本8pxに抑え、スポーツ/コートライン由来のシャープさを出す
7. Hero / Career / Final CTA を3大見せ場として作り込む

